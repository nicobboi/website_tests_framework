Vedi 'git help <comando>' per saperne di più su un sottocomando specifico

Comandi principali di alto livello
   add                  Aggiunge i contenuti del file all'indice
   am                   Applica una serie di patch da una casella di posta
   archive              Crea un archivio di file da un albero con nome
   bisect               Usa la ricerca binaria per trovare il commit che ha introdotto un bug
   branch               Elenca, crea o elimina branch
   bundle               Sposta oggetti e riferimenti in base a un archivio
   checkout             Passa da un branch a un altro o ripristina i file nell'albero di lavoro
   cherry-pick          Applica le modifiche introdotte da alcuni commit esistenti
   citool               Alternativa grafica a git-commit
   clean                Rimuove i file non tracciati dall'albero di lavoro
   clone                Clona un repository in una nuova directory
   commit               Registra modifiche al repository
   describe             Attribuisce un nome leggibile a un oggetto basandosi su un riferimento disponibile
   diff                 Visualizza le modifiche fra commit, un commit e l'albero di lavoro, ecc.
   fetch                Scarica oggetti e riferimenti da un altro repository
   format-patch         Prepara patch per il loro invio tramite e-mail
   gc                   Ripulisce file non necessari e ottimizza il repository locale
   gitk                 Il browser per i repository Git
   grep                 Stampa le righe corrispondenti a un'espressione regolare
   gui                  Un'interfaccia grafica portabile per Git
   init                 Crea un repository Git vuoto o ne reinizializza uno esistente
   log                  Visualizza i log dei commit
   maintenance          Esegui delle attività per ottimizzare i dati dei repository Git
   merge                Unisce due o più cronologie di sviluppo
   mv                   Sposta o rinomina un file, una directory o un collegamento simbolico
   notes                Aggiunge o esamina le note agli oggetti
   pull                 Esegue il fetch e l'integrazione con un altro repository o un branch locale
   push                 Aggiorna i riferimenti remoti insieme agli oggetti associati
   range-diff           Compara due intervalli di commit (ad es. due versioni di un branch)
   rebase               Riapplica dei commit dopo l'ultimo commit di un branch
   reset                Ripristina l'HEAD corrente allo stato specificato
   restore              Ripristina i file nell'albero di lavoro
   revert               Esegue il revert di alcuni commit esistenti
   rm                   Rimuove file dall'albero di lavoro e dall'indice
   shortlog             Riassume l'output di 'git log'
   show                 Visualizza vari tipi di oggetti
   sparse-checkout      Inizializza e modifica sparse-checkout
   stash                Mette da parte le modifiche in una directory di lavoro sporca
   status               Visualizza lo stato dell'albero di lavoro
   submodule            Inizializza, aggiorna o esamina sottomoduli
   switch               Passa da un branch a un altro
   tag                  Crea, elenca, elimina o verifica un oggetto tag firmato con GPG
   worktree             Gestisce alberi di lavoro multipli

Comandi / operazioni di manipolazione ausiliari
   config               Ottiene e imposta le opzioni a livello di repository o globali
   fast-export          Esportatore di dati Git
   fast-import          Backend per importatori di dati Git veloci
   filter-branch        Riscrive branch
   mergetool            Esegue gli strumenti di risoluzione conflitti di merge per risolvere i conflitti di merge
   pack-refs            Esegue il pack di head e tag per un accesso efficiente al repository
   prune                Elimina tutti gli oggetti non raggiungibili dal database oggetti
   reflog               Gestisce le informazioni del registro dei riferimenti
   remote               Gestisce l'insieme dei repository tracciati
   repack               Esegue il pack degli oggetti non sottoposti a pack in un repository
   replace              Crea, elenca, elimina i riferimenti per sostituire oggetti

Comandi / operazioni di interrogazione ausiliari
   annotate             Annota le righe dei file con le informazioni sui commit
   blame                Visualizza quali revisione e autore hanno modificato ogni singola riga di un file per ultimi
   bugreport            Raccogli le informazioni necessarie perché l'utente possa inviare una segnalazione d'errore
   count-objects        Conta il numero di oggetti non in un pack e la loro occupazione di spazio su disco
   difftool             Visualizza le modifiche usando tool comuni per la visualizzazione dei diff
   fsck                 Verifica la connessione e la validità degli oggetti nel database
   gitweb               Interfaccia Web per Git (frontend Web ai repository Git)
   help                 Visualizza una guida a Git
   instaweb             Sfoglia istantaneamente il tuo repository di lavoro su gitweb
   merge-tree           Visualizza un merge a tre vie senza modificare l'indice
   rerere               Riusa la risoluzione registrata dei merge che hanno generato conflitti
   show-branch          Visualizza i branch e i loro commit
   verify-commit        Verifica la firma GPG dei commit
   verify-tag           Verifica la firma GPG dei tag
   whatchanged          Visualizza i registri insieme alle differenze introdotte da ciascun commit

Interazione con altri
   archimport           Importa un repository GNU Arch in Git
   cvsexportcommit      Esporta un singolo commit in un checkout CVS
   cvsimport            Recupera i dati da un sistema di controllo di versione che le persone adorano odiare
   cvsserver            Un emulatore di server CVS per Git
   imap-send            Invia un insieme di patch dallo standard input a una cartella IMAP
   p4                   Importa dati da, o invia dati a, repository Perforce
   quiltimport          Applica un insieme di patch quilt sul branch corrente
   request-pull         Genera un riassunto delle modifiche in sospeso
   send-email           Invia un insieme di patch come e-mail
   svn                  Operatività bidirezionale tra un repository Subversion e Git

Comandi / operazioni di manipolazione di basso livello
   apply                Applica una patch ai file e/o all'indice
   checkout-index       Copia dei file dall'indice all'albero di lavoro
   commit-graph         Scrive e verifica i file grafo dei commit di Git
   commit-tree          Crea un nuovo oggetto commit
   hash-object          Calcola l'ID oggetto e crea facoltativamente un blob da un file
   index-pack           Genera il file indice pack per un archivio esistente sottoposto a pack
   merge-file           Esegue un merge a tre vie su file
   merge-index          Esegue un merge per i file che lo richiedono
   mktag                Creates a tag object with extra validation
   mktree               Genera un oggetto albero da testo in formato ls-tree
   multi-pack-index     Scrive e verifica indici multi-pack
   pack-objects         Crea un archivio di oggetti sottoposto a pack
   prune-packed         Rimuove gli oggetti aggiuntivi già presenti nei file pack
   read-tree            Legge le informazioni su un albero dall'indice
   symbolic-ref         Legge, modifica ed elimina riferimenti simbolici
   unpack-objects       Decomprime gli oggetti da un archivio sottoposto a pack
   update-index         Registra i contenuti dei file dell'albero di lavoro nell'indice
   update-ref           Aggiorna in modo sicuro il nome oggetto salvato in un riferimento
   write-tree           Crea un oggetto albero dall'indice corrente

Comandi / operazioni di interrogazione di basso livello
   cat-file             Fornisci informazioni sul contenuto o sul tipo e sulle dimensioni per gli oggetti di un repository
   cherry               Trova commit che devono essere ancora applicati upstream
   diff-files           Confronta file nell'albero di lavoro e nell'indice
   diff-index           Confronta un albero con l'albero di lavoro o l'indice
   diff-tree            Confronta il contenuto e i mode dei blob trovati tramite due oggetti albero
   for-each-ref         Visualizza in output le informazioni su ogni riferimento
   for-each-repo        Run a Git command on a list of repositories
   get-tar-commit-id    Estrae l'ID del commit da un archivio creato usando git-archive
   ls-files             Visualizza informazioni sui file nell'indice e nell'albero di lavoro
   ls-remote            Elenca i riferimenti in un repository remoto
   ls-tree              Elenca i contenuti di un oggetto albero
   merge-base           Trova il maggior numero possibile di antenati comuni per un merge
   name-rev             Trova i nomi simbolici per le revisioni date
   pack-redundant       Trova file pack ridondanti
   rev-list             Elenca gli oggetti commit in ordine cronologico inverso
   rev-parse            Sceglie e altera i parametri
   show-index           Visualizza l'indice di un archivio sottoposto a pack
   show-ref             Elenca i riferimenti in un repository locale
   unpack-file          Crea un file temporaneo con i contenuti di un blob
   var                  Visualizza una variabile logica di Git
   verify-pack          Convalida i file archivio sottoposti a pack di Git

Comandi di basso livello / Sincronizzazione repository
   daemon               Un server veramente semplice per repository Git
   fetch-pack           Riceve oggetti mancanti da un altro repository
   http-backend         Implementazione lato server di Git su HTTP
   send-pack            Esegue il push di oggetti su un altro repository tramite il protocollo Git
   update-server-info   Aggiorna il file informazioni aggiuntive per aiutare i server stupidi

Comandi di basso livello / Funzioni helper interne
   check-attr           Visualizza le informazioni sugli attributi Git
   check-ignore         Esegui il debug dei file gitignore/escludi file
   check-mailmap        Visualizza i nomi canonici e gli indirizzi e-mail dei contatti
   check-ref-format     Si assicura che un nome riferimento sia ben formato
   column               Visualizza i dati in colonne
   credential           Recupera e salva credenziali utente
   credential-cache     Helper per salvare temporaneamente password in memoria
   credential-store     Helper per salvare credenziali su disco
   fmt-merge-msg        Genera un messaggio di commit del merge
   interpret-trailers   Aggiunge o analizza informazioni strutturate nei messaggi di commit
   mailinfo             Estrae la patch e le informazioni sull'autore da un singolo messaggio e-mail
   mailsplit            Un semplice programma per suddividere i file UNIX mbox
   merge-one-file       Il programma helper standard da usare con git-merge-index
   patch-id             Calcola l'ID univoco per una patch
   sh-i18n              Codice di inizializzazione dell'internazionalizzazione di Git per gli script shell
   sh-setup             Codice comune di inizializzazione di Git per gli script shell
   stripspace           Rimuove spazi bianchi non necessari
