PREREQUISITES:
	docker: v24.0.4
	docker compose: v2.20.0
Versions used inside the containers:
	poetry: v1.5.1
	node: 19.6.0
	npm: v9.7.2

GUIDE:
- git clone
- copy .env file from .env.template and change the project configuration
- docker-compose up

If needed, launch npm install into the celeryworker container for mauve and pa-website-validator

localhost:5050/
pgadmin db setup:
	- add new server
	- in "General" set the db name
	- in "Connection":
		- "Host name/address" = "db"
		- "Username" and "Password" are in the .env file

localhost/docs
API
