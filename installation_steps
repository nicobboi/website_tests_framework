- git clone
- docker-compose up
- docker-compose exec backend bash
	- cd alembic/versions --> remove all revisions
	- cd ../..
	- alembic revision --autogenerate -m <msg>
	- alembic upgrade head
<!-- If needed, launche npm install into the celeryworker container for mauve and pa-website-validator -->
